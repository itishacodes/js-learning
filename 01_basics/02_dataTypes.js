"use strict";  // treat all JS code as a newer version

// alert("hello") 
// node me alert pop-up nhi aata
// we are using node JS, not browser

let name = "Itisha" // string
let age = 18 // integer
let isLoggedIn = false // boolean
let gender;

// bigInt --> for large numbers
// null ---> standalone value
// undefined ---> representation of an empty value

console.table([typeof name, typeof age, typeof isLoggedIn, typeof null, typeof gender])